Chapter 22 Lists, Stacks, Queues, and Priority Queues

4.	You can use index to traverse elements in an ArrayList.
a. true
b. false
Key:a

#
5.	You should use the ArrayList or LinkedList to store duplicate elements.
a. true
b. false
Key:a

#
6.	The elements in an ArrayList can be traversed using ListIterator or using index.
a. true
b. false
Key:a

#
7.	All the update methods in Vector are synchronized.
a. true
b. false
Key:a

#
8.	All the update methods in the Java Collection framework are synchronized.
a. true
b. false
Key:b Only the update methods in Vector and Stack are synchronized.

#
9.	ListIterator is a subclass of Iterator. 
a. true
b. false
Key:b ListIterator is a subinterface of Listerator

#
11.	You can sort an array using the sort method in the Arrays class.
a. true
b. false
Key:a

#
15.	What will be displayed by the following code?
    ArrayList<Integer> list = new ArrayList<Integer>();
    list.add(0);
    list.add(1);
    list.add(2);
    list.add(1, 4);
    list.set(2, 30);
    System.out.println(list);
a. [0, 1, 2, 4, 30]
b. [0, 4, 2, 30]
c. [0, 4, 30, 2]
d. [0, 1, 4, 30]
e. [4, 1, 2, 30]

Key:c

#
16.	What will be displayed by the following code?
import java.util.*;

public class Test  {
  public static void main(String[] args) throws Exception {
    ArrayList<Integer> list = new ArrayList();
    list.add(1); list.add(2); list.add(1); list.add(3);
    remove(list, 1);
    System.out.println(list);
  }
  
  public static void remove(ArrayList list, int value) {
    int k = 0;
    while (k < list.size()) {
      if (list.get(k).equals(value))
        list.remove(k);
      k++;
    }
  }
}
a. [1, 2, 1, 3]
b. [2, 1, 3]
c. [1, 2, 3]

d. [2, 3]

Key:d

#
29. The poll() method in the Queue interface
a. retrieves and removes the head of this queue, or null if this queue is empty.
b. retrieves and removes the head of this queue and throws an exception if this queue is empty.
c. retrieves, but does not remove, the head of this queue, returning null if this queue is empty.
d. retrieves, but does not remove, the head of this queue, throwing an exception if this queue is empty.
Key:a

#
29. The remove() method in the Queue interface
a. retrieves and removes the head of this queue, or null if this queue is empty.
b. retrieves and removes the head of this queue and throws an exception if this queue is empty.
c. retrieves, but does not remove, the head of this queue, returning null if this queue is empty.
d. retrieves, but does not remove, the head of this queue, throwing an exception if this queue is empty.
Key:b

#
29. The peek() method in the Queue interface
a. retrieves and removes the head of this queue, or null if this queue is empty.
b. retrieves and removes the head of this queue and throws an exception if this queue is empty.
c. retrieves, but does not remove, the head of this queue, returning null if this queue is empty.
d. retrieves, but does not remove, the head of this queue, throwing an exception if this queue is empty.
Key:c

#
29. The element() method in the Queue interface ____________________
a. retrieves and removes the head of this queue, or null if this queue is empty.
b. retrieves and removes the head of this queue and throws an exception if this queue is empty.
c. retrieves, but does not remove, the head of this queue, returning null if this queue is empty.
d. retrieves, but does not remove, the head of this queue, throwing an exception if this queue is empty.
Key:d
